# Anything else

- Every element slug exists in this site's registry.
- Every property path exists on that element.
- Nothing hardcoded that has a variable.
- Heading levels descend in order.
- Images have alt text.
- Nothing nested deeper than it needs to be.
- Dry run first, recommendations shown, then write.
