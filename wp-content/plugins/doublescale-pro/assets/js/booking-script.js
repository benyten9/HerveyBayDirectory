/**
 * Public booking page (confirm / cancel / reschedule / claim_waitlist) script stub.
 *
 * Loaded via wp_enqueue_script( 'doublescale-booking-page' ) from
 * BookingFrontendHandler::enqueue_scripts(). Action page templates currently
 * inline their own JS (e.g. the cancel-booking AJAX handler in cancel.php), so
 * this file exists to satisfy the registration without producing 404 noise.
 * Add shared booking-page behavior here if/when templates need it.
 */
(function () {
	// Intentionally empty - placeholder for shared booking page behavior.
})();
